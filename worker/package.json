{
  "name": "scam-slayer-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Secret proxy for Scam Slayer — keeps Venice/RPC/Etherscan keys off the client",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.84.0"
  }
}
